package lms_126

fun task11(): String {

    return """
        IF unit is Celsius separate the unit from the magnitude
        IF unit is Fahrenheit separate the unit from the magnitude
        
        IF C convert to F
        ELSE
        IF F convert to C
        OUTPUT result of conversion 
    """
}
