package lms_126

fun task9(): String {

    return """
        INPUT card to ATM
        INPUT card code
        INPUT amount to take out
        IF amount is > balance OUTPUT error
      ELSE if amount is < or the same
      OUTPUT  chosen amount
      OUTPUT payslip & card to user 
    """
}
